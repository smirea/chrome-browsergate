{
  "name": "chrome-browsergate",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run scripts/build.ts --watch",
    "install-extension": "bun run scripts/install.ts",
    "invoke": "bun run scripts/invoke",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "latest",
    "@types/yargs": "latest",
    "typescript": "latest"
  }
}
