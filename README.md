# Browser Gate, a chrome extension

Acts as a local automatic bridge to the browser for scripts and AIs to use

Common use-cases:
- fetch session tokens from tabs
- setup automations to run on urls (sort of like a programatic tampermonkey)
